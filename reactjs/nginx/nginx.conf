# Nginx가 index.html 파일을 루트 경로로 제공
# React 라우팅과 관련된 모든 요청을 index.html로 되돌려 보내 SPA(단일 페이지 애플리케이션)가 정상적으로 작동하게 함
server {
  listen 80;

  location / {
    root /usr/share/nginx/html;
    index index.html index.htm;
    try_files $uri /index.html;
  }
}